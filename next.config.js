/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['drive.google.com']
    },
    output: 'export',
    reactStrictMode: true,
}

module.exports = nextConfig
